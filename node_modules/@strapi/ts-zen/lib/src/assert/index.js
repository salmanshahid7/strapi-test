"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createAssertTypeSelector = void 0;
const matchers_1 = require("../matchers");
function createAssertTypeSelector(symbols, diagnostics, checker, sourceFile, program) {
    function typeSelector(typeName) {
        const context = { symbols, diagnostics, checker, sourceFile, program };
        const { matchers, matchersUtils } = (0, matchers_1.createWrappedMatchers)(typeName, context);
        // Make sure the matchers' state has a consistent default value
        matchersUtils.setState({ isNot: false });
        return matchers;
    }
    return typeSelector;
}
exports.createAssertTypeSelector = createAssertTypeSelector;
//# sourceMappingURL=index.js.map