import * as React from 'react';
import { TooltipPosition } from './utils/positionTooltip';
import { BoxProps } from '../Box';
export interface TooltipProps extends Omit<BoxProps<'div'>, 'position'> {
    description?: string;
    delay?: number;
    id?: string;
    label?: React.ReactNode;
    position?: TooltipPosition;
}
export declare const Tooltip: ({ children, label, description, delay, position, id, ...props }: TooltipProps) => import("react/jsx-runtime").JSX.Element;
