import { BoxProps } from '../Box';
export interface DialogProps extends BoxProps {
    onClose: () => void;
    title: string;
    isOpen: boolean;
}
export declare const Dialog: ({ onClose, title, as, isOpen, id, zIndex, ...props }: DialogProps) => import("react/jsx-runtime").JSX.Element | null;
