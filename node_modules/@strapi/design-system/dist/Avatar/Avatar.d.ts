import * as React from 'react';
import { FlexProps } from '../Flex';
import { TypographyProps } from '../Typography';
export interface AvatarProps {
    /**
     * Alternative text
     */
    alt?: string;
    /**
     * Image src of the image preview (displayed on `Avatar` hover).
     */
    preview?: boolean | string;
    /**
     * Image src of the `Avatar`
     */
    src: string;
}
export declare const Avatar: ({ src, alt, preview }: AvatarProps) => import("react/jsx-runtime").JSX.Element;
export type InitialsProps = Pick<FlexProps, 'background'> & Pick<TypographyProps, 'textColor'> & {
    children: React.ReactNode;
};
export declare const Initials: ({ children, background, textColor }: InitialsProps) => import("react/jsx-runtime").JSX.Element;
