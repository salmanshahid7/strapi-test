import { DefaultTheme } from 'styled-components';
import { TEXT_VARIANTS } from './constants';
export declare const ellipsisStyle: ({ ellipsis }: {
    ellipsis?: boolean;
}) => false | "\n    display: block;\n    white-space: nowrap;\n    overflow: hidden;\n    text-overflow: ellipsis;\n  ";
export declare const variantStyle: ({ variant, theme, }: {
    variant?: (typeof TEXT_VARIANTS)[number];
    theme: DefaultTheme;
}) => string;
