import React from 'react';
import { FlexProps } from '../../Flex';
export interface NavSectionProps extends FlexProps<'ul'> {
    children: React.ReactNode;
    label: string;
    horizontal?: boolean;
    spacing?: number;
}
export declare const NavSection: ({ label, children, horizontal, spacing, ...props }: NavSectionProps) => import("react/jsx-runtime").JSX.Element;
