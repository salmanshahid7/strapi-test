import { MutableRefObject } from 'react';
interface UseIntersectionOptions {
    selectorToWatch: string;
    skipWhen?: boolean;
}
export declare const useIntersection: (scrollableAreaRef: MutableRefObject<HTMLElement | null>, callback: (entry: IntersectionObserverEntry) => void, { selectorToWatch, skipWhen }: UseIntersectionOptions) => void;
export {};
