/**
 * TODO: Look into why setting the type to `BaseCheckboxSize` makes the
 * component in `BaseCheckbox` loose all type information to do with the theme.
 */
export declare const getCheckboxSize: ({ size }: {
    size: any;
}) => "20px" | "18px";
