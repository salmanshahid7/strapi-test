import { DefaultTheme } from 'styled-components';
import { FlexProps } from '../Flex';
export type BadgeSize = 'S' | 'M';
export interface BadgeProps extends FlexProps {
    /**
     * If `true`, it changes the `backgroundColor` to `primary200` and the `textColor` to `primary600`
     */
    active?: boolean;
    backgroundColor?: keyof DefaultTheme['colors'];
    size?: BadgeSize;
    textColor?: keyof DefaultTheme['colors'];
}
export declare const Badge: ({ active, size, textColor, backgroundColor, children, minWidth, ...props }: BadgeProps) => import("react/jsx-runtime").JSX.Element;
