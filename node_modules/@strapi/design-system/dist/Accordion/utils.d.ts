import { DefaultTheme } from 'styled-components';
import { AccordionVariant } from './Accordion';
export declare const getBackground: ({ expanded, disabled, variant, }: {
    expanded: boolean;
    disabled: boolean;
    variant: AccordionVariant;
}) => keyof DefaultTheme['colors'];
