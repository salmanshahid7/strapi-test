import { BoxProps } from '../Box';
type Enumerate<N extends number, Acc extends number[] = []> = Acc['length'] extends N ? Acc[number] : Enumerate<N, [...Acc, Acc['length']]>;
type IntRange<F extends number, T extends number> = Exclude<Enumerate<T>, Enumerate<F>>;
export interface ProgressBarProps extends Omit<BoxProps, 'background' | 'hasRadius' | 'height' | 'position' | 'width'> {
    max?: IntRange<0, 101>;
    min?: IntRange<0, 101>;
    size?: 'S' | 'M';
    value?: IntRange<0, 101>;
}
export declare const ProgressBar: ({ min, max, value, children, size, ...props }: ProgressBarProps) => import("react/jsx-runtime").JSX.Element;
export {};
