import { FlexProps } from '../Flex';
export interface TagProps extends FlexProps<'button'> {
    icon: React.ReactNode;
}
export declare const Tag: ({ children, icon, disabled, onClick, ...props }: TagProps) => import("react/jsx-runtime").JSX.Element;
