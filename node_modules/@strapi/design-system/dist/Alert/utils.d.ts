import { DefaultTheme } from 'styled-components';
import type { AlertVariant } from './Alert';
interface StyledProps {
    theme: DefaultTheme;
    variant: AlertVariant;
}
export declare const handleBackgroundColor: (variant: AlertVariant) => "primary100" | "danger100" | "success100" | "warning100";
export declare const handleBorderColor: (variant: AlertVariant) => string;
export declare const handleIconColor: ({ theme, variant }: StyledProps) => string;
export {};
